---
description: GitHubWallpaper — стек, архитектура, текущий этап
alwaysApply: true
---

# GitHubWallpaper — контекст

Динамические обои: HTML за иконками рабочего стола + данные GitHub REST API.

**Репозиторий:** [rkfsociety/GitHubWallpaper](https://github.com/rkfsociety/GitHubWallpaper) · **Платформы:** v1.0 Windows 10/11 · v2.0 Windows + Linux · **Ответы:** только на русском

## Стек

**v1.0:** C# / .NET 8 · WinForms · WebView2 · WorkerW (`DesktopHost`) · HTML/CSS/JS в `wwwroot/wallpaper/`  
**v2.0:** Python 3.11+ · PySide6 · Qt WebEngine · `keyring` · каталог `python/`

## Текущий этап (ROADMAP)

| Этап | Статус |
|------|--------|
| 1 Каркас движка | ✅ |
| 2 GitHub + один репо | ✅ |
| 3 Мульти-репо и настройки | ✅ |
| 4 Полная активность | ✅ |
| 5 Полировка и релиз (C# v1) | ✅ |
| 6 Python + Qt v2.0 | 🔄 |

**Сейчас:** v1.0 — push в `main` → CI обновляет Release `latest` (portable exe). v2.0 — ветка `python-qt`, см. `ROADMAP.md` этап 6.

## Git (агент)

После каждой завершённой задачи: **коммит и сразу push** в `main`. Не спрашивать подтверждение. Без push CI не соберёт новый release.

## Архитектура

```
TrayService / SettingsForm
        → WallpaperController → DesktopHost (WorkerW) → WebView2 (app.local)
        → RepoPoller → GitHubApiClient → Bridge (JSON → WebView2)
PAT: GitHubPatCredentialStore (Credential Manager)
Репо: SettingsStore → settings.json
```

## Ключевые пути

| Что | Где |
|-----|-----|
| Точка входа | `src/Program.cs`, `TrayApplicationContext.cs` |
| Обои | `src/Desktop/` — `WallpaperController`, `Bridge`, `DesktopHost` |
| GitHub | `src/GitHub/` — `GitHubApiClient`, `RepoPoller`, `RepoUrlParser` |
| Настройки | `src/Settings/` — `SettingsForm`, `SettingsStore`, `GitHubPatCredentialStore` |
| UI обоев | `wwwroot/wallpaper/` — `index.html`, `app.js`, `styles.css` |
| Python v2.0 | `python/` — PySide6-приложение (ветка `python-qt`) |
| AppData | `%APPDATA%\GitHubWallpaper\` (`AppPaths.AppData`, `settings.json`) |
| Публикация | `src/Properties/PublishProfiles/win-x64.pubxml` → `publish/` |

## Bridge: типы сообщений (C# → JS)

`pause` · `resume` · `auth:status` · `repos:init` · `repo:metadata` · `repo:commits` · `repo:pulls` · `repo:issues` · `repo:releases` · `repo:ci-run` · `repo:heatmap` · `repo:activity-feed` · `repo:poll-failed` · (JS→C#) `open-url`

Virtual host: `app.local` (`WallpaperController.VirtualHostName`).

## Соглашения кода

- PAT **никогда** в `settings.json` — только Credential Manager
- `internal sealed` для сервисов; XML-doc на публичных API
- Минимальный diff; не трогать несвязанный код
- Недостающие SDK/инструменты — в `F:\github\PROG`

## Сборка

```bash
dotnet run --project src
dotnet publish src -p:PublishProfile=win-x64
```

Требуется WebView2 Runtime на машине пользователя.
