# Память проекта GitHubWallpaper

Правила для агента — `.cursor/rules/*.mdc` (`alwaysApply: true`).

| Файл | Назначение |
|------|------------|
| `project-context.mdc` | Стек, архитектура, текущий этап, соглашения |
| `agent-workflow.mdc` | Git, сборка, язык ответов |
| `roadmap.mdc` | Как вести ROADMAP.md и README |

Источники истины: `ROADMAP.md`, `README.md`, код в `src/` и `wwwroot/wallpaper/`.
