# Память проекта GitHubWallpaper

Правила для агента — `.cursor/rules/*.mdc` (`alwaysApply: true`).

| Файл | Назначение |
|------|------------|
| `project-context.mdc` | Стек (Python-only), структура репо, архитектура, пути, сборка |
| `agent-workflow.mdc` | Git (коммит всегда, push по запросу), тесты, релиз |
| `roadmap.mdc` | Как вести ROADMAP.md; текущий фокус этапа 6 |

**Источники истины:** `ROADMAP.md`, `README.md`, код в `python/` и `wwwroot/wallpaper/`.

**Активная разработка:** только `python/` (PySide6). История предыдущей версии — в git-тегах `v1.0.x`.
